## webpack 5 boilerplate settings (single entry or multiple entries) 
- install packages: `yarn`  
- build files: `yarn build`  
- run develop: `yarn start`  
  
webpack config files: `/scripts/webpack.config.js`